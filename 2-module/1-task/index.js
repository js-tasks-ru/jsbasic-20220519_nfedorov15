function sumSalary(salaries) {
  let salaries = {
    John: 1000,
    Ann: 1600,
    Pete: 1300,
    month: 'December',
    currency: 'USD',
    isPayed: false
  }
    
  function sumSalaries (salaries){
    
    
  let sum = 0; 
  for (let key in salaries){
  }
  for (typeof salaries[key] == 'number'){
  }
  for (isFinite(salaries[key])){
  }
  for (!Number.isNaN(salaries[key])){
           
    sum += salaries[key];
  }
  }
}
  
  return (sum);
    
  
    sumSalaries(salaries);

